---
apiVersion: v1
kind: Service
metadata:
  name: result
  namespace: default
  labels:
    app: cloud-project
spec:
  type: ClusterIP
  selector:
    app: result
  ports:
  - port: 8080
    targetPort: 4000