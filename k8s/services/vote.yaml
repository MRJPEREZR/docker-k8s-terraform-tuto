---
apiVersion: v1
kind: Service
metadata:
  name: vote
  namespace: default
  labels:
    app: cloud-project
spec:
  type: ClusterIP
  selector:
    app: vote
  ports:
  - port: 80
    targetPort: 5000